namespace GB1.Domain.Enums
{
    public enum Severidade
    {
        Informativo,  // Mensagens informativas, sem impacto
        Baixo,        // Baixa severidade, impacto mínimo
        Medio,        // Severidade média, requer atenção
        Alto,         // Alta severidade, impacto relevante
        Critico       // Severidade crítica, requer ação imediatapublic Severidade() 
    }
}
